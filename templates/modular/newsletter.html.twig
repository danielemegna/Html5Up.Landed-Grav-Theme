<!-- Five -->
  <section id="five" class="wrapper style2 special fade">
    <div class="container">
      <header>
        <h2>{{ page.header.title }}</h2>
        {{ page.content }}
      </header>
      <form method="post" action="#" class="container 50%">
        <div class="row uniform 50%">
          <div class="8u 12u$(xsmall)"><input type="email" name="email" id="email" placeholder="{{ page.header.textbox_placeholder }}" /></div>
          <div class="4u$ 12u$(xsmall)"><input type="submit" value="{{ page.header.button_label }}" class="fit special" /></div>
        </div>
      </form>
    </div>
  </section>